=========================INSTRUCTIONS=========================

Use the code below to import all the required libraries
pip install -r requirements.txt

Then it is sufficient to open the prompt in the folder and digit:

python CVRP_MIP.py instXX

Where XX refers to the selected instance number. 

Then, at the first input, 1 stands for MTZ model; 2 for GSEC model.
