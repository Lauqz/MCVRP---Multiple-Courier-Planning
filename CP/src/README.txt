=========================INSTRUCTIONS=========================

Use the code below to import all the required libraries
pip install -r requirements.txt

Then you can launch program directly from the terminal, 
it takes as input the instance that you want to solve.

python ./minizinc_MCP_Hamiltonian.py instXX

Where XX refers to the selected instance number.